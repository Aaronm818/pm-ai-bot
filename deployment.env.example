# Azure Container Registry Deployment Configuration
# Copy this file and customize it with your ACR details

# Required: Azure Container Registry Configuration
# Replace 'myregistry' with your actual ACR name
ACR_NAME=myregistry

# Replace with your actual ACR login server
# Format: <registry-name>.azurecr.io
ACR_LOGIN_SERVER=myregistry.azurecr.io

# Optional: Image Configuration
# The name of your Docker image in the registry
IMAGE_NAME=acs-teams-recording

# Version tag for your image (can be latest, v1.0.0, etc.)
VERSION=latest

# Target platforms for multi-architecture builds
# Common options:
# - linux/amd64 (Intel/AMD x64)
# - linux/arm64 (ARM64, including Apple M1/M2)
# - linux/arm/v7 (ARM v7)
PLATFORMS=linux/amd64,linux/arm64

# Example configuration:
# If your ACR is named "mycompany-acr", your values would be:
# ACR_NAME=mycompany-acr
# ACR_LOGIN_SERVER=mycompany-acr.azurecr.io
